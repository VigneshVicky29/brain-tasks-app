server {
    listen 3000;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # React app uses client-side routing
        try_files $uri /index.html;
    }
}
